/*
森林全般に関する情報を出力するメソッドを作ってみよう。
まず、forestFactsという静的な文字列フィールドを定義します。
forestFactsは値で初期化しないでください。

次に、ゲッターだけ（セッターなし）のForestFactsという名前のプライベート静的プロパティを定義する。 ForestFactsも値で初期化しないでください。

最後に、ForestFactsの値をコンソールに書き出すPrintForestFacts()という名前のパブリック静的メソッドを定義します。 ForestFactsには値が代入されないことに注意してください。
*/

using System;

namespace StaticMethods
{
  class Program
  {
    static void Main(string[] args)
    {

    }
  }
}
